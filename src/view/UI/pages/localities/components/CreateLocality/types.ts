export interface ICreateLocality {
  showModal: boolean;
  handleCloseCreateModal: () => void;
}

export interface ICreateLocalityModal {
  showModal: boolean;
  handleCloseCreateModal: () => void;
}
